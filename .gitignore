# Nested repo — reference only, not part of this vault
everything-claude-code/

# Environment
.env

# OS
.DS_Store
Thumbs.db

# Claude Code worktrees
.claude/worktrees/

# MCP config — contains Local REST API bearer token
.mcp.json

# Local REST API plugin settings — contains API key + TLS private key
.obsidian/plugins/obsidian-local-rest-api/data.json
